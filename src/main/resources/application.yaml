server.context-path: /travelmanager 
---
spring:
  profiles: development

  data:
    mongodb:
      database: meineDatenbank
---
spring:
  profiles: test

  data:
    mongodb:
      database: testDB
  
---
spring:
  profiles: openshift

  data:
    mongodb:
      uri: ${MONGO_URL}
      database: meineProdDB
  