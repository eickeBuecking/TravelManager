

spring:
  profiles: development

  data:
    mongodb:
      datasbase: meineDatenbank
---
spring:
  profiles: test

  data:
    mongodb:
      database: testDB
  
---
spring:
  profiles: openshift

  data:
    mongodb:
      uri: ${MONGO_URL}
      database: sampledb
  